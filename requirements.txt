# Core Scientific Computing
numpy
pandas
scipy
matplotlib
colormaps  # Note: not on conda-forge, installed via pip in Colab

# Geospatial/GIS Libraries
# Note: In Colab, these are installed via pip after apt-get installs system libraries.
# The notebook setup handles the apt dependencies automatically.
rasterio
pyproj
geopandas
shapely
rioxarray
# gdal - not needed directly, rasterio provides GDAL bindings

# Point Cloud Processing
# Note: In Colab, PDAL requires system libraries installed via apt-get first.
# The notebook setup runs: apt-get install pdal libpdal-dev libpdal-plugins
pdal
# small_gicp - optional, excluded from Colab setup

# Performance Optimization
numba

# Web and Data Access
requests

# Interactive Jupyter Widgets
ipyleaflet
ipywidgets

# Configuration
pyyaml
